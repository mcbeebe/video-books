/**
 * Returns a friendly greeting. Bootstrap placeholder — exists only to prove
 * the workspace + TypeScript + Vitest plumbing works end-to-end. Replaced by
 * `packages/types` in Phase 1 task #2.
 *
 * @param name - The name to greet.
 * @returns A greeting of the form `hello, <name>`.
 * @example
 *   hello('world'); // 'hello, world'
 */
export function hello(name: string): string {
  return `hello, ${name}`;
}
